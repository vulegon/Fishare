import React from "react";
import { Header } from "features/header";
import { SideBar } from "features/sideBar";

const Home: React.FC = () => {
  return (
    <>
     <Header/>
     <SideBar/>
    </>
  );
};

export default Home;
